import {
    StyleSheet,
    Text,
    View,
    SafeAreaView,
    Image,
    KeyboardAvoidingView,
    TouchableOpacity,
  } from "react-native";

export default function LoginPage({ navigation }) {
    return(
        <View>
            <Text>RegisterScreen</Text>
        </View>
    );
}